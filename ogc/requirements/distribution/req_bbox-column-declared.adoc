// Source: distributing-geoparquet.md (PR #302), When to add a bbox covering column; gpio check bbox
[[req_distribution_bbox-column-declared]]
[requirement]
====
[%metadata]
identifier:: /req/distribution/bbox-column-declared
part:: If the file contains a root-level group column with child fields named `xmin`, `ymin`, `xmax` and `ymax`, that column SHALL be declared as the bounding box covering of a geometry column (<<rc_covering>>).
====
